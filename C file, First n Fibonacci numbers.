#include<stdio.h>

int main()
{
    int no1=0, no2=1, fib, i,n;
    printf("enter the value of n");
    scanf("%d",&n);
    for(i=1;i<=n; i++)
    {
    fib=no1+no2;
    printf("%d\n",no1);
    no1=no2;
    no2=fib;
    
    
    }
    return 0;
}
