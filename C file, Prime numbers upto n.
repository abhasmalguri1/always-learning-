#include <stdio.h>

#include<math.h>
 void main()
 {

 int n,x;

 printf("Enter the value of n:");
 scanf("%d",&n);
 printf("Prime numbers between 1 and %d are \n",n);
 for (int i=1; i<=n;i++)
 {x=1;
 for(int j=2;j<(i);j++)
{
if(i%j==0)
{x=0;
break;
}
}
if(x==1)
{ printf("%d\n",i);
}
}
}
